# The lesson's breakdown

```text
Members / Principals
├── Google Account
├── Service Account
├── Google Group
├── Google Workspace Domain
└── Cloud Identity Domain

IAM Policies
├── Bindings
│   └── Principal(s) + Role
├── Allow Policies
├── Deny Policies
└── IAM Conditions

Identity Administration
├── Cloud Identity / Google Workspace
├── Google Cloud Directory Sync
└── Single Sign-On

Policy Hierarchy
└── Organization → Folder → Project → Resource
```
>
> IAM grants access, but IAM does not create/manage users and groups. The course says those identities are managed through Google Workspace or Cloud >
> Identity.
>
